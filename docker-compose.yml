# remove or ignore the top-level version: line to silence that warning
services:
  stream:
    build: .
    restart: always
    env_file:
      - .env
    volumes:
      - ./html:/app/html:ro
      - ./screenshots:/app/screenshots
      - ./start.sh:/app/start.sh:ro